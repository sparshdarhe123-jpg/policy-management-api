package com.sparshdarhe.policyapi;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Entry point for the Insurance Policy Management API.
 * Run locally with: mvn spring-boot:run
 */
@SpringBootApplication
public class PolicyApiApplication {

    public static void main(String[] args) {
        SpringApplication.run(PolicyApiApplication.class, args);
    }
}
